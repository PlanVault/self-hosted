{
  "annotations": { "list": [] },
  "editable": false,
  "id": null,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "up{job=\"litellm\"}", "legendFormat": "up", "refId": "A" }],
      "title": "LiteLLM scrape up",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(litellm_proxy_total_requests_metric_total[5m]))",
          "legendFormat": "requests",
          "refId": "A"
        }
      ],
      "title": "LiteLLM request rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        {
          "expr": "sum(rate(litellm_proxy_failed_requests_metric_total[5m])) / clamp_min(sum(rate(litellm_proxy_total_requests_metric_total[5m])), 0.001)",
          "legendFormat": "failure ratio",
          "refId": "A"
        }
      ],
      "title": "LiteLLM failure rate",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["planvault", "litellm"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "LiteLLM",
  "uid": "planvault-litellm",
  "version": 1
}
